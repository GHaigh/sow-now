{
  "$schema": "./node_modules/wrangler/config-schema.json",
  "name": "sow-now-api",
  "routes": [
    { "pattern": "api.sow-now.uk/*", "zone_name": "sow-now.uk" }
  ],
  "main": "src/index.ts",
  "compatibility_date": "2025-05-01",
  "compatibility_flags": ["nodejs_compat"],

  // ── Top-level vars (production defaults) ──────────────────────────────────
  "vars": {
    "ENVIRONMENT": "production"
  },

  "observability": {
    "enabled": true
  },

  // ── D1 Database ────────────────────────────────────────────────────────────
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "vernal-db",
      "database_id": "ea16b6f5-180e-4a00-ae79-c03daff06bc8",
      "migrations_dir": "../../migrations"
    }
  ],

  // ── KV — sessions & subscription tier cache ────────────────────────────────
  "kv_namespaces": [
    {
      "binding": "SESSIONS",
      "id": "920877451b624246ba8616b811951d73"
    }
  ],

  // ── Email sending (magic links) ────────────────────────────────────────────
  "send_email": [
    {
      "name": "EMAIL",
      "allowed_sender_addresses": ["hello@sow-now.uk"]
    }
  ],

  // ── Queue — advice generation jobs ─────────────────────────────────────────
  "queues": {
    "producers": [
      {
        "binding": "ADVICE_QUEUE",
        "queue": "vernal-advice-queue"
      }
    ],
    "consumers": [
      {
        "queue": "vernal-advice-queue",
        "max_batch_size": 10,
        "max_batch_timeout": 30,
        "max_retries": 3
      }
    ]
  },

  // ── Durable Objects — per-device GDD state ─────────────────────────────────
  "durable_objects": {
    "bindings": [
      {
        "name": "DEVICE_STATE",
        "class_name": "DeviceStateDO"
      }
    ]
  },
  "migrations": [
    {
      "tag": "v1",
      "new_sqlite_classes": ["DeviceStateDO"]
    }
  ],

  // ── Workers AI ─────────────────────────────────────────────────────────────
  "ai": {
    "binding": "AI"
  },

  // ── Vectorize — crop knowledge base ────────────────────────────────────────
  "vectorize": [
    {
      "binding": "CROP_INDEX",
      "index_name": "vernal-crops"
    }
  ],

  // ── Cron triggers — GDD engine runs at 05:30 UTC daily ────────────────────
  "triggers": {
    "crons": ["30 5 * * *"]
  },

  // ── Environments ───────────────────────────────────────────────────────────
  "env": {
    "staging": {
      "name": "sow-now-api-staging",
      "vars": {
        "ENVIRONMENT": "staging"
      }
    },
    "production": {
      "name": "sow-now-api",
      "vars": {
        "ENVIRONMENT": "production"
      }
    }
  }
}
