{
  "name": "sow-now",
  "version": "0.1.0",
  "private": true,
  "description": "Sow Now — Precision GDD platform for home growers",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=apps/api\" \"npm run dev --workspace=apps/web\"",
    "build": "npm run build --workspace=apps/api && npm run build --workspace=apps/web",
    "deploy": "npm run deploy --workspace=apps/api && npm run deploy --workspace=apps/web",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "db:migrate:local": "wrangler d1 migrations apply vernal-db --local --config apps/api/wrangler.jsonc",
    "db:migrate": "wrangler d1 migrations apply vernal-db --remote --config apps/api/wrangler.jsonc",
    "db:seed:local": "wrangler d1 execute vernal-db --local --file=./scripts/seed.sql --config apps/api/wrangler.jsonc",
    "db:seed": "wrangler d1 execute vernal-db --remote --file=./scripts/seed.sql --config apps/api/wrangler.jsonc",
    "cf:setup": "node ./scripts/cf-setup.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wrangler": "^4.20.0",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "allowScripts": {
    "workerd@1.20250906.0": true,
    "workerd@1.20260714.1": true,
    "fsevents@2.3.3": true,
    "sharp@0.33.5": true,
    "sharp@0.34.5": true,
    "esbuild@0.28.1": true,
    "esbuild@0.25.4": true,
    "esbuild@0.25.12": true
  }
}
