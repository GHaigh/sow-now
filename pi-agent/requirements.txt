# Sow Now Pi Agent — Python dependencies
# Install into venv: pip install -r requirements.txt

# Async HTTP client — for uplink to Cloudflare Workers
httpx==0.28.1

# Note: all sensor decoding is handled by the rtl_433 system binary (installed
# via apt in setup/install.sh). No Python radio/GPIO libraries are needed.
